export const PORTFOLIO = {
  INITIAL_EQUITY: 10000,
  PRICE_MIN: 0.01,
  PRICE_MAX: 0.99,
  PRICE_MID: 0.5,
}

export const CYCLE = {
  INTERVAL_MS: 5 * 60 * 1000,
  TUNE_EVERY_N_CYCLES: 12,
  TUNE_INTERVAL_MS: 60 * 60 * 1000,
  PRINT_INTERVAL_MS: 10000,
  PERSIST_EVERY_N_TICKS: 60,
}

export const SIGNAL = {
  COST_BPS: 20,
  MIN_EV_BPS: 5,
  CONFIDENCE_THRESHOLD: 0.1,
  TTL_MS: 3000,
  IMBALANCE_THRESHOLD: 0.3,
}

export const RISK = {
  INTRADAY_STOP_PCT: 0.05,
  MAX_DRAWDOWN_PCT: 0.2,
  MAX_POSITION_PCT: 0.1,
}

export const CACHE = {
  DEFAULT_TTL_MS: 5 * 60 * 1000,
  MARKET_DATA_TTL_MS: 60 * 1000,
  SEMANTIC_TTL_MS: 30 * 60 * 1000,
}

export const API = {
  RATE_LIMIT_WINDOW_MS: 60 * 1000,
  RATE_LIMIT_MAX_REQUESTS: 100,
}
